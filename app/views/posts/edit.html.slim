- title t('posts.edit.page_title', title: @post.title)
h1 = t('.title')
= render 'form'
= link_to t('.show'), @post, class:"btn btn-xs btn-success mt-3" if policy(@post).show?
= link_to t('.back'), posts_path, class:"btn btn-xs btn-info mt-3 ml-3"
