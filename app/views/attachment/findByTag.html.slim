main
  .row
    = render(partial: 'left_col')
    .group_images
      - @attachments.each do |image|
        - if @findIdByTag.include? image.id
          .image_block
            = link_to attachment_path(image.id) do
              = image_tag image.file.url, class: 'thumb', alt: "#{image.file.identifier}"
